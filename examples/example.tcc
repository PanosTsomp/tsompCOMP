// A simple calculator program in tsompCC

// Function to add two numbers
int add(int a, int b) {
    return a + b;
}

// Main function
int main() {
    int x = 10;
    int y = 20;
    int result = add(x, y);

    // Print the result
    print(result);

    return 0;
}

#ifndef TSOMPCC_TOKENS_H
#define TSOMPCC_TOKENS_H

enum TokenType {
    INT = 256,
    FLOAT,
    BOOL,
    VOID,
    IF,
    ELSE,
    WHILE,
    FOR,
    RETURN,
    PRINT,
    NUMBER,
    FLOATNUMBER,
    IDENTIFIER,
    EQ,
    NEQ,
    LE,
    GE,
    AND,
    OR
};

#endif // TSOMPCC_TOKENS_H